name: Embed Research Papers

on:
  push:
    branches:
      - main
    paths:
      - 'sustainability-vercel/app/rag/papers/**/*.pdf'
  workflow_dispatch:

jobs:
  embed:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        sudo apt-get update && sudo apt-get install -y poppler-utils libgl1 tesseract-ocr build-essential python3-dev
        python -m pip install --upgrade pip
        pip install -r sustainability-vercel/app/rag/vector_requirements.txt
        python -m spacy download en_core_web_sm

    - name: Run embedding script
      run: python sustainability-vercel/app/rag/rag_helpers/embed_papers.py